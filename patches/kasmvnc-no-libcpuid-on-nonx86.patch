diff -urN orig/build.sh new/build.sh
--- orig/build.sh	1970-01-01 05:00:01.000000000 +0500
+++ new/build.sh	2026-02-16 17:40:14.645246067 +0500
@@ -101,7 +101,14 @@
   
   # Configure X.Org with proper library flags
   echo "Configuring X.Org server..."
-  LDFLAGS="${LDFLAGS:-} -lz -lpng -ltbb -ljpeg -lfmt -lcpuid" ./configure \
+  EXTRA_XORG_LIBS="-lz -lpng -ltbb -ljpeg -lfmt"
+  case "$(uname -m)" in
+    x86_64|i?86)
+      EXTRA_XORG_LIBS="${EXTRA_XORG_LIBS} -lcpuid"
+      ;;
+  esac
+
+  LDFLAGS="${LDFLAGS:-} ${EXTRA_XORG_LIBS}" ./configure \
     --disable-config-hal \
     --disable-config-udev \
     --disable-dmx \
@@ -195,4 +202,4 @@
 echo "  mkdir -p ~/.vnc"
 echo "  openssl req -x509 -nodes -days 3650 -newkey rsa:2048 -keyout ~/.vnc/self.pem -out ~/.vnc/self.pem -subj '/C=US/ST=VA/L=None/O=None/OU=DoFu/CN=kasm/emailAddress=none@none.none'"
 echo "  ./xorg.build/bin/Xvnc -interface 0.0.0.0 -disableBasicAuth -Log *:stdout:100 -sslOnly 1 -cert ~/.vnc/self.pem -key ~/.vnc/self.pem :1"
-echo ""
\ No newline at end of file
+echo ""
diff -urN orig/common/rfb/CMakeLists.txt new/common/rfb/CMakeLists.txt
--- orig/common/rfb/CMakeLists.txt	1970-01-01 05:00:01.000000000 +0500
+++ new/common/rfb/CMakeLists.txt	2026-02-16 17:40:20.171214992 +0500
@@ -140,7 +140,15 @@
 
 find_package(PkgConfig REQUIRED)
 
-pkg_check_modules(CPUID REQUIRED libcpuid)
+string(TOLOWER "${CMAKE_SYSTEM_PROCESSOR}" KASMVNC_CPU_ARCH)
+if (KASMVNC_CPU_ARCH MATCHES "^(x86_64|amd64|i[3-6]86)$")
+    pkg_check_modules(CPUID REQUIRED libcpuid)
+else ()
+    message(STATUS "libcpuid disabled for non-x86 architecture: ${CMAKE_SYSTEM_PROCESSOR}")
+    set(CPUID_INCLUDE_DIRS "")
+    set(CPUID_LIBRARIES "")
+endif ()
+
 pkg_check_modules(FMT REQUIRED fmt)
 pkg_check_modules(VIDEO_ACCELERATION REQUIRED libva libva-drm libdrm)
 
diff -urN orig/common/rfb/cpuid.cxx new/common/rfb/cpuid.cxx
--- orig/common/rfb/cpuid.cxx	1970-01-01 05:00:01.000000000 +0500
+++ new/common/rfb/cpuid.cxx	2026-02-16 17:40:51.437054898 +0500
@@ -72,9 +72,10 @@
 }; // namespace rfb
 
 namespace cpu_info {
-    static rfb::LogWriter log("CpuFeatures");
     inline CpuFeatures::CpuFeatures()
     {
+#if defined(__x86_64__) || defined(__i386__)
+        static rfb::LogWriter log("CpuFeatures");
         if (!cpuid_present())
         {
             log.error("CPU does not support CPUID.");
@@ -93,5 +94,6 @@
         {
             log.error("Cannot identify CPU.");
         }
+#endif
     }
 } // namespace cpu_info
diff -urN orig/common/rfb/cpuid.h new/common/rfb/cpuid.h
--- orig/common/rfb/cpuid.h	1970-01-01 05:00:01.000000000 +0500
+++ new/common/rfb/cpuid.h	2026-02-16 17:40:40.776106534 +0500
@@ -20,7 +20,12 @@
 #define __RFB_CPUID_H__
 
 #include <algorithm>
+#include <cstdint>
+#include <thread>
+
+#if defined(__x86_64__) || defined(__i386__)
 #include <libcpuid/libcpuid.h>
+#endif
 
 namespace cpu_info {
     //using namespace cpu_features;
@@ -32,7 +37,9 @@
     bool supportsAVX512f();
 
     class CpuFeatures {
+#if defined(__x86_64__) || defined(__i386__)
         cpu_id_t data{};
+#endif
         CpuFeatures();
 
     public:
@@ -47,25 +54,89 @@
             return instance;
         }
 
-        [[nodiscard]] bool has_sse2() const { return data.flags[CPU_FEATURE_SSE2]; }
+        [[nodiscard]] bool has_sse2() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_SSE2];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_sse4_1() const { return data.flags[CPU_FEATURE_SSE4_1]; }
+        [[nodiscard]] bool has_sse4_1() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_SSE4_1];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_sse4_2() const { return data.flags[CPU_FEATURE_SSE4_2]; }
+        [[nodiscard]] bool has_sse4_2() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_SSE4_2];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_sse4a() const { return data.flags[CPU_FEATURE_SSE4A]; }
+        [[nodiscard]] bool has_sse4a() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_SSE4A];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_avx() const { return data.flags[CPU_FEATURE_AVX]; }
+        [[nodiscard]] bool has_avx() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_AVX];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_avx2() const { return data.flags[CPU_FEATURE_AVX2]; }
+        [[nodiscard]] bool has_avx2() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_AVX2];
+#else
+            return false;
+#endif
+        }
 
-        [[nodiscard]] bool has_avx512f() const { return data.flags[CPU_FEATURE_AVX512F]; }
+        [[nodiscard]] bool has_avx512f() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return data.flags[CPU_FEATURE_AVX512F];
+#else
+            return false;
+#endif
+        }
 
         [[nodiscard]] bool has_smt() const { return get_total_cpu_count() > get_cores_count(); }
 
-        [[nodiscard]] uint16_t get_total_cpu_count() const { return std::max(1, data.total_logical_cpus); }
+        [[nodiscard]] uint16_t get_total_cpu_count() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return std::max<uint16_t>(1, data.total_logical_cpus);
+#else
+            const auto n = std::thread::hardware_concurrency();
+            return n > 0 ? static_cast<uint16_t>(n) : static_cast<uint16_t>(1);
+#endif
+        }
 
-        [[nodiscard]] uint16_t get_cores_count() const { return std::max(1, data.num_cores); }
+        [[nodiscard]] uint16_t get_cores_count() const
+        {
+#if defined(__x86_64__) || defined(__i386__)
+            return std::max<uint16_t>(1, data.num_cores);
+#else
+            return get_total_cpu_count();
+#endif
+        }
     };
 
     inline static const bool has_sse2 = CpuFeatures::get().has_sse2();
